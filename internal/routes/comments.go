package routes

import (
	"github.com/gin-gonic/gin"
)

func RegisterCommentsRoutes(rg *gin.RouterGroup, d Deps) {
	// rg.GET("/posts/:id/comments", handlers.GetCommentsByPost(d.Models.Comments))
	// rg.POST("/posts/:id/comments", handlers.CreateComment(d.Models.Comments))
	// rg.PUT("/posts/:id/comments/:commentId", handlers.UpdateComment(d.Models.Comments))
	// rg.DELETE("/posts/:id/comments/:commentId", handlers.DeleteComment(d.Models.Comments))
}
